localgov_expired_events.settings:
  type: config_object
  label: 'Expired Event module settings'
  mapping:
    localgov_expired_events:
      type: mapping
      mapping:
        action:
          type: boolean
          label: 'Action type'
        expire_days:
          type: int
          label: 'number of days'
        item_per_cron:
          type: int
          label: 'Items per cron'